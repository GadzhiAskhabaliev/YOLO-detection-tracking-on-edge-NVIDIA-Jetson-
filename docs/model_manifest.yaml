# Список кандидатов на сравнение. Дополняй по ходу (пути к весам, метрики, статус).

models:
  - name: yolov8n_crowdhuman
    status: первый смоук после поднятия инстанса
    weights_path: models/yolov8n_crowdhuman.pt

  # Дальше по мере подключения: fcos_r50, yolox_tiny, crowddet, freeyolo, pedestron, vpd_atss, fairmot ...
