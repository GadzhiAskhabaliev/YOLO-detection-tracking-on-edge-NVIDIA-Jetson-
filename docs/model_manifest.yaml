# Candidate models for benchmarking; extend with weights_path, metrics, status.

models:
  - name: yolov8n_crowdhuman
    status: smoke test after instance bootstrap
    weights_path: models/yolov8n_crowdhuman.pt

  # Planned entries: fcos_r50, yolox_tiny, crowddet, freeyolo, pedestron, vpd_atss, fairmot ...
